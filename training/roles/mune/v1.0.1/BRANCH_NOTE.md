Canonical training-source branch for this package: `training/mune-debugger-verification-v1.0.1`.

The version directory itself is immutable training source. Operational checkpoints live outside the frozen role meaning.
