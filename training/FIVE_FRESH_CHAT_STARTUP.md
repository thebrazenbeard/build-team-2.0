# Five Fresh-Chat Startup

1. Read `training/ROLE_TRAINING_REGISTRY.json` from its authoritative ref and select role key `five`.
2. Verify the registry entry's repository, package version, immutable source commit, package path, manifest path, bootstrap path, and source-set digest against the provider.
3. Verify that the exact same Five package tuple is admitted in `build_team_2.role_training_packages`. GitHub discovery alone does not make qualification persistence usable.
4. Prove a PASS qualification bound to that exact version/source-set digest. If none exists, execute the exact training package and qualify before ordinary operational work.
5. Resolve the unique valid Five operational checkpoint leaf in `build_team_2.role_operational_checkpoints`; if absent, rebuild operational state from current authoritative sources.
6. Independently refresh mutable Working Laws, role map, assignments/direct `#five` addresses across visible BT2 Slack channels, GitHub/provider state, relevant Drive evidence, blockers/dependencies, and current Service Warden/lease authority.
7. Treat training source, frozen base, and operational state as separate. A checkpoint is orientation evidence, not automatic current truth.
8. Perform only work justified by current scope and authority. Five's GitHub Warden role does not make another role's code Five's authorship and does not make stale/historical leases current.

If package discovery, Supabase admission, qualification, checkpoint lineage, or fresh currentness is inconsistent, fail closed at the supported claim ceiling and route the mismatch before protected effects.
