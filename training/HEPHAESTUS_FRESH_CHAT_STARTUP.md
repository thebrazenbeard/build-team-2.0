# Hephaestus Fresh-Chat Startup

This file is a discoverability pointer. It does not replace the versioned training manifest and grants no operational authority.

## Training source

Use `training/roles/hephaestus/REGISTRATION.json` to resolve the exact immutable training package source. For the currently registered package, retrieve the package from its recorded immutable commit and execute its `BOOTSTRAP.md` and `TRAINING_MANIFEST.yaml` exactly as declared.

Do not treat the current `main` branch, this startup file, repository access, or the existence of helper scripts as permission to mutate GitHub or any other service.

## Training checkpoint support

The registration also binds the optional checkpoint-support source. Use it only to verify package bytes and preserve resumable training-execution state. A training checkpoint is derivative evidence about a training run. It is not the training source, a frozen base, or project operational state.

## BASE_READY gate

A chat becomes `BASE_READY` only by satisfying the exact conditions in the registered package manifest and receipt template. A checkpoint cannot bypass a failed or incomplete module or final qualification.

## After BASE_READY

Freeze/preserve the qualified chat as the role base. Ordinary work should occur in a working branch/chat derived from that base.

Before operational action, freshly retrieve and reconcile:

- current native Project Instructions;
- current BT2 governance and permanent-role map;
- current assignments/direct identity addresses;
- current Service Warden and bounded lease authority;
- exact provider/source/device state relevant to the assignment;
- the latest valid operational checkpoint, if one exists.

Saved checkpoints and base training do not prove current provider state, current authority, uninterrupted runtime, or subjective continuity.
